import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'br.com.inteligenciatitan.app',
  appName: 'Titan IA',
  webDir: 'www',
  // Não precisamos mais do plugin EdgeToEdge aqui, pois o CSS cuida disso
};

export default config;